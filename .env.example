# Points the frontend at the backend API. For local dev, leave as-is.
# For a Vercel deployment, set VITE_API_URL in the Vercel project's env vars
# to the deployed backend URL, e.g. https://your-app.onrender.com/api
VITE_API_URL=http://localhost:3001/api
